import { px2rem } from '~/utils';
import { Shapes } from '~/utils/types';

export default {
  smallRadius: px2rem(4),
  mediumRadius: px2rem(8),
  largeRadius: px2rem(16),
  modalRadius: px2rem(12),
  buttonRadius: px2rem(40),
} as Shapes;
